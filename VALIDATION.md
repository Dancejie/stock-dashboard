# 交易看板 1.0.3 发布与验证

更新日期：2026-09-15。原 Cowork 作品 **34711 已成功更新至 1.0.3**。

- 使用入口：[交易看板](https://cowork.xiaohongshu.com/s/b36220f2/)
- 作品管理：[原作品管理页](https://cowork.xiaohongshu.com/app/NWEzY2U5YmE)
- 实际浏览器中的双股组合、WFA 和测试配置清理已完成；最后发布细化了 Alpha / WFA 对应策略标签。

最终复查：`online-final-readonly.json` 显示 5 个核心线上脚本与当前源码逐字节一致；工作区版本 4，原空内容已恢复且在重新部署后保留。业务版本 `2026.09.15-strategies.4`，部署包 SHA-256 `1d3035f1c610a72376fb2ddb08093e6f0827d697f7da8cf2476781b9de0f439b`。本段为发布后的验收记录，不影响部署业务源码。

## 1. 本次功能

手动平均成本、持仓/可卖股数、自选快速切换和显式保存；组合同区间回测支持不等权初始资金分配与固定股数估值，展示现金、净值及股票贡献。手动成本不改写历史收益，资金比例模式不隐含每日再平衡。

9 种技术策略为均值回归、均线交叉、海龟、布林带、TD、网格，以及新增的 **Supertrend、TSMOM 入场波动定仓、55 日突破加吊灯退出**。买入持有另作基线。三种新增策略的公式、默认参数和研究改编范围见 `STRATEGY_RESEARCH.md`。

## 2. 已完成的验证

| 验证层次 | 结果 | 证据或范围 |
| --- | --- | --- |
| Node | **88 / 88 通过** | `final-node-tests.log`：因果信号、成交账本、组合/WFA、三个新增策略、行情客户端、搜索隔离及工作台 |
| Python / ASGI | **23 / 23 通过** | `final-python-tests.log`：身份、用户隔离、版本冲突、输入及行情清洗等；合计 **111 项测试通过** |
| JSDOM | **8 个流程通过，13 次模拟请求** | `final-dashboard-integration.log`：启动、自选保存重载、切股、持仓计划、单股回测、WFA、组合、新增策略计划与回测 |
| 发布包检查 | **30 项 PASS，失败 0** | 最新 `package-verification-final.log`；此前部署已实际完成 install、start、health |
| 当前行情客户端对齐 | **5813 项断言通过** | `client-live-parity.json`、`client-live-parity.cjs`：四组真实序列各 242 根，检查日期、OHLCV、条数及股票名称 |
| 真实 PostgreSQL 工作区 | **保存、重载、旧版本 409、测试记录清理通过** | `workspace-online-verification.json`；冲突后原数据未被覆盖 |
| 跨部署保留 | **通过** | `online-version3-check.json`：同一 SSO 身份的原工作区内容保持，新增静态资源与客户端适配器已生效 |
| 实际 Chrome 页面 | **已列流程通过** | 真实报价；“长白山”名称搜索；自选快速跳转；平均成本 30 / 持仓 100 的保存并重载；9 种策略逐一执行比较验证 |
| 双股组合 | **两种模式通过实际页面及独立对账** | 默认费用/整手资金组合 746 项断言通过，固定股数结果与原样本精确匹配；详见第 4 节 |
| WFA | **真实浏览器运行与展示通过** | 固定参数、训练/验证区间分离，未显示“策略有效”；详见第 4 节 |
| 浏览器测试清理 | **通过** | 临时组合从未保存并已删除；测试持仓已移除并保存；API 复查工作区 version 4、自选 0、组合 0，原空工作区已恢复 |
| 移动布局 | **390 像素宽度下可滚动到达操作区域** | 实际浏览器检查；不扩大为所有设备与交互均已验收 |

JSDOM 使用模拟行情、工作区及图表接口；真实浏览器、数据库与行情证据单独列出，不能互相替代。浏览器验收记录见 `browser-acceptance.md`。上述证据文件保存在仓库旁的 `stock-dashboard-audit-20260915` 审计目录；本文不包含 Cookie、身份正文、数据库凭据或用户工作区快照。

测试命令（本次 guard 源码目录）：

```sh
node --test tests/*.test.cjs
python3 -m unittest discover -s tests -p 'test_*.py'
```

JSDOM 从审计目录执行：

```sh
node dashboard-integration.cjs '../stock-dashboard-upgrade-20260915-guard'
```

## 3. 当前线上行情与搜索路径

Cowork 容器访问公网行情曾发生超时。当前 `market-client.js` 由浏览器通过 CORS 请求腾讯公开行情，并提供固定东方财富接口作为后备；行情结果经过类型、日期、OHLC 和复权口径校验，不在获取不到复权数据时暗中改用 raw。

名称搜索的固定 JSONP 接口仅在 `sandbox="allow-scripts"`、不具备同源权限的 iframe 中执行。父页面校验消息来源、随机标识与返回字段，不在持仓页面执行远程代码或 `eval` 远程响应。此设计已由测试及真实名称搜索路径验证。

当前客户端与保存的真实行情逐项对齐：兴业银锡 `sz000426`、长白山 `sh603099`，各取 qfq/raw，区间 **2025-09-15—2026-09-14**，四组各 242 根。5813 项包括日期、条数和名称断言，不全部是数值断言。它验证了数据传输及清洗的一致性，不代表行情供应商永远可用或策略未来收益可靠。

浏览器行情依赖用户网络、供应商 CORS 与接口可用性；后备路径存在不等于所有供应商故障场景都已在线触发验证。服务端公网行情超时的历史记录保留在 `online-verification.json`，其总体失败状态不能被当作全部线上验证通过；修复后的证据以当前客户端和浏览器结果为准。

## 4. 真实数据回测与独立算术证据

### 新增三个策略：保留全部结果

`advanced-strategy-market.json` 保存两只股票各 **725 根三年前复权数据**，实际最后日线为 2026-09-14；三年数据用于预热。`advanced-strategy-live-check.json` 的实际计分区间为 **2025-09-15—2026-09-14**，每场景初始资金 10 万元，收盘信号、次日开盘执行模型。

| 股票 | Supertrend | TSMOM 入场定仓 | 突破 + 吊灯 |
| --- | ---: | ---: | ---: |
| 兴业银锡 | -19.35% | +8.25% | +3.94% |
| 长白山 | -24.60% | -7.00% | -2.05% |

6 个结果全部保留，包括 4 个亏损场景。这里证明代码可用真实数据运行，不据此宣布策略有效；不是三年收益，也不是完整独立逐笔账本复算。

### 原双股组合对账

原 `portfolio-live-check.json` 的 **1485 次独立算术断言**已针对当前 guard 重跑通过，证据为 `final-portfolio-fixture-check.log` / `.json`。两种模式逐日净值最大误差均为 0，实际共同区间同为上述 242 个日期：

| 场景 | 独立计算与引擎一致的结果 |
| --- | --- |
| 10 万元，初始分配 60% / 30%，10% 保留现金 | 期末 115,965.295 元；+15.965295% |
| 固定 1000 / 400 股，额外现金 5000 元 | 50,622 元 → 55,430 元；+9.497846786% |

该算术对账的资金比例场景使用 qfq、零费用和 1 股单位，固定股数使用 raw；都未建立分红及送转事件账本。当前客户端对齐另外核对了四组原始验证数据没有发生数值变化。

### 实际浏览器双股组合及默认交易设置

- 资金比例：10 万元，长白山 30%、兴业银锡 60%、10% 保留现金，两股均买入持有，使用页面默认费用和整手设置；期末 **116,476.55 元，+16.48%**。独立检查 **746 项断言通过**，5 个 UI 数值精确到分一致，242 日净值最大浮点误差 `2.91e-11`。证据：`final-ui-portfolio-defaults-check.cjs` / `.json` / `.log`。
- 固定股数：长白山 400 股、兴业银锡 1000 股，额外现金 5000 元；页面显示 **50,622 元 → 55,430 元，+9.50%**，与原样本独立计算精确匹配。
- 两个临时组合均未保存，检查后已删除；手动测试持仓已移除并保存，恢复空工作区。

默认费用/整手场景与前述零费用/1 股场景交易数量不同，期末资产不同是预期结果，不能混作同一组对账。

### 实际浏览器 WFA

Supertrend 使用真实行情，评价区间 2025-09-15—2026-09-14。训练段 **2025-09-15—2026-05-22，-21.11%**；验证段 **2026-05-25—2026-09-14，+0.80%**。页面明确展示固定参数，未按训练收益宣布有效。验证段已完成交易数为 **0**，正收益来自未平仓市值变化，不能解释为一次成功的已平仓交易。

## 5. 部署方式与验证范围

本次保留官方生成的安装模板。旧 CLI 预检硬编码的镜像要求，与官方模板接受平台环境注入镜像的方式冲突，因此使用兼容强制部署选项；随后 **30 项包检查通过，平台实际安装、启动与健康检查完成**。这不是旧预检所有步骤无条件通过的记录。部署过程见 `redeploy-compatible.log`、`redeploy-client.log` 与包检查日志。

外部 `/health` 返回的是 **Cowork 平台 guard** 的健康状态，不能单独证明业务行情、身份或持仓接口成功；这些功能依据上文实际请求和浏览器验证。

验证范围仍有一项身份限制：两个独立真实 SSO 身份的交叉隔离未单独验收；当前已完成单身份真实持久化，用户隔离另有单元/ASGI 测试。不能把单身份验证扩大为双身份实测。API canary 和后续浏览器测试配置均已清理。

## 6. 计算和执行限制

复权策略是研究模拟，固定 raw 股数模式是价格收益，均不等同于含分红、送转等公司行为的真实券商流水。涨跌停、申报单位、历史交易规则、排队与部分成交仍有简化。

TSMOM 只在入场时定仓，15% 不是持续或保证波动；吊灯的风险比例只用于数量估算，不能限制跳空损失。条件单为可复制计划，不连接券商、不自动下单；盘中碰价和收盘确认是不同执行规则。研究来源及这些改编边界保持见 `STRATEGY_RESEARCH.md`。
